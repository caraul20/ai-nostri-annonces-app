// Firebase temporar dezactivat - folosim date mock
console.log('Firebase dezactivat - folosim date mock');

// Mock objects pentru a evita erorile
export const auth = null;
export const db = null;
export const storage = null;

export default null;
